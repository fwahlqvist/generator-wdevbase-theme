language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-wdevbase-theme' ]; then cd .. && eval "mv $currentfolder generator-wdevbase-theme" && cd generator-wdevbase-theme; fi
